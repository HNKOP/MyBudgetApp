<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@drawable/back2">


    <EditText
        android:id="@+id/edit_newlogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите имя пользователя"
        android:layout_marginTop="40dp"
        android:layout_margin="15dp"/>
    <EditText
        android:id="@+id/edit_newpass"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите пароль"
        android:layout_below="@+id/edit_newlogin"
        android:layout_margin="15dp"/>
    <EditText
        android:id="@+id/edit_newpass_again"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Повторите пароль"
        android:layout_below="@+id/edit_newpass"
        android:layout_margin="15dp"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Зарегистрироваться"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/edit_newpass_again"
        android:onClick="buttonConfirmRegOnClick"
        android:layout_margin="15dp"
        android:background="@drawable/auth_login_button"
        />
</RelativeLayout>